{% extends '::base.html.twig' %}
{% set title='Individu' %}
{% set titre='Individu' %}
{% set soustitre='Détail Individu' %}

{% block body -%}
    <h1>Individu.</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ entity.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
        </tbody>
    </table>

    {% include "DomiAdresseBundle:Adresse:listeSimple.html.twig" %}

        <ul class="record_actions">
    <li>
        <a href="{{ path('individu') }}">
            Back to the list (src/Domi/Bundle/AdresseBundle/Resources/views/Individu/show.html.twig:35)
        </a>
    </li>
    <li>
        <a href="{{ path('individu_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
